package com.example.gagym.diarysend.diary;

import com.example.gagym.diarysend.Result;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class DiaryCreateResponse {
	private DiarySend diary;
	private Result result; // {"code": ..., "message": "정사적으로 생성되었습니다."}
}
